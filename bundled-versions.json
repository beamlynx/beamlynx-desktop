{
  "_comment": "Exact git refs (tag, branch, or SHA) of pine-lang and beamlynx-ui bundled into this desktop build. Checked out directly by CI via these refs -- update deliberately as part of a release, per the beamlynx-desktop release checklist in AGENTS.md. Currently pinned to the desktop-app branches because the fixes required for desktop packaging (classpath-based grammar load, Clerk gating) aren't in a tagged pine-lang/beamlynx-ui release yet -- switch to specific version tags once those land in a real release.",
  "pineLangRef": "desktop-app",
  "beamlynxUiRef": "desktop-app"
}

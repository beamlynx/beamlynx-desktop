# Count

The count operation returns the total number of rows in a result set. Use 'count:' to get the row count of your query results.

**Operation(s):** `count:`

**Syntax:**
```
table_name | [operations...] | count:
```

## Examples

```

```
